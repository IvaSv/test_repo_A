# Port for the API to run on
server.port=${PORT:8080}

# Root path ("context path") for all backend requests
server.servlet.context-path=/api

# Database configuration
spring.datasource.password=${DB_PASS:b5N3mDCgjMxYOIl55cpS1as1Jd5uTXQ3}
spring.datasource.username=${DB_USERNAME:probna_baza_user}
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-cl8atb2uuipc73eoqi30-a:5432/probna_baza}
spring.datasource.driverClassName=${DB_DRIVER:org.postgresql.Driver}

# SQL initialization mode
spring.sql.init.mode=always

# Hibernate configuration
spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Hibernate DDL mode
spring.jpa.hibernate.ddl-auto=update

# Show SQL in logs
spring.jpa.show-sql=true

